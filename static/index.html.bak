<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PTRE — Traffic Management Center</title>
    <meta name="description"
        content="Proactive Traffic Routing Engine — AI-Powered Operator Decision Support for Trafikverket">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link
        href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;700&family=Inter:wght@400;500;600;700&display=swap"
        rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        mono: ['JetBrains Mono', 'monospace'],
                        sans: ['Inter', 'system-ui', 'sans-serif'],
                    },
                    colors: {
                        tmc: {
                            bg: '#0a0e17',
                            panel: '#111827',
                            border: '#1e293b',
                            accent: '#334155',
                            text: '#e2e8f0',
                            muted: '#64748b',
                        },
                        urgency: {
                            immediate: '#ef4444',
                            soon: '#f59e0b',
                            advisory: '#3b82f6',
                        },
                        status: {
                            online: '#22c55e',
                            verified: '#22c55e',
                            failed: '#ef4444',
                            pending: '#f59e0b',
                            expired: '#64748b',
                        }
                    }
                }
            }
        }
    </script>
    <style>
        body {
            background: #0a0e17;
        }

        .pulse-online {
            animation: pulse-green 2s ease-in-out infinite;
        }

        @keyframes pulse-green {

            0%,
            100% {
                opacity: 1;
                box-shadow: 0 0 4px #22c55e;
            }

            50% {
                opacity: 0.6;
                box-shadow: 0 0 12px #22c55e;
            }
        }

        .panel-glow {
            box-shadow: 0 0 0 1px rgba(51, 65, 85, 0.5), 0 4px 24px rgba(0, 0, 0, 0.4);
        }

        .scanline {
            background: linear-gradient(transparent 50%, rgba(0, 0, 0, 0.04) 50%);
            background-size: 100% 4px;
            pointer-events: none;
        }

        .terminal-scroll {
            scrollbar-width: thin;
            scrollbar-color: #334155 transparent;
        }

        .terminal-scroll::-webkit-scrollbar {
            width: 6px;
        }

        .terminal-scroll::-webkit-scrollbar-track {
            background: transparent;
        }

        .terminal-scroll::-webkit-scrollbar-thumb {
            background: #334155;
            border-radius: 3px;
        }

        .urgency-flash-immediate {
            animation: flash-red 1s ease-in-out infinite;
        }

        @keyframes flash-red {

            0%,
            100% {
                border-color: #ef4444;
            }

            50% {
                border-color: #7f1d1d;
            }
        }

        .fade-in {
            animation: fadeIn 0.3s ease-in;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(4px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .modal-backdrop {
            backdrop-filter: blur(8px);
        }
    </style>
</head>

<body class="font-sans text-tmc-text min-h-screen">
    <!-- ═══════════════ HEADER ═══════════════ -->
    <header class="border-b border-tmc-border bg-tmc-panel/80 backdrop-blur-sm sticky top-0 z-50">
        <div class="max-w-[1920px] mx-auto px-6 py-3 flex items-center justify-between">
            <div class="flex items-center gap-4">
                <div class="flex items-center gap-2">
                    <div class="w-2 h-2 rounded-full bg-status-online pulse-online" id="status-dot"></div>
                    <span class="text-xs font-mono text-status-online uppercase tracking-wider" id="status-text">System
                        Online</span>
                </div>
                <div class="h-4 w-px bg-tmc-border"></div>
                <h1 class="text-sm font-semibold tracking-wide">
                    <span class="text-white">PTRE</span>
                    <span class="text-tmc-muted mx-1">—</span>
                    <span class="text-tmc-muted font-normal">Proactive Traffic Routing Engine</span>
                    <span class="text-tmc-muted mx-1">|</span>
                    <span class="text-tmc-muted font-normal">Trafik Stockholm Copilot</span>
                </h1>
            </div>
            <div class="flex items-center gap-4">
                <button id="btn-datex2"
                    class="px-3 py-1.5 text-xs font-mono bg-tmc-accent/50 hover:bg-tmc-accent border border-tmc-border rounded transition-colors">
                    Export DATEX II (NTS)
                </button>
                <div class="flex items-center gap-2 text-xs font-mono text-tmc-muted">
                    <span id="tick-counter">TICK #—</span>
                    <div class="h-3 w-px bg-tmc-border"></div>
                    <span id="live-clock">--:--:--</span>
                </div>
            </div>
        </div>
    </header>

    <!-- ═══════════════ METRICS BAR ═══════════════ -->
    <div class="border-b border-tmc-border bg-tmc-panel/40">
        <div class="max-w-[1920px] mx-auto px-6 py-2 flex items-center gap-6">
            <div class="flex items-center gap-2">
                <span class="text-[10px] font-mono text-tmc-muted uppercase">Incidents</span>
                <span class="text-sm font-mono font-bold text-white" id="metric-incidents">0</span>
            </div>
            <div class="h-3 w-px bg-tmc-border"></div>
            <div class="flex items-center gap-2">
                <span class="text-[10px] font-mono text-tmc-muted uppercase">VMS Alerts</span>
                <span class="text-sm font-mono font-bold text-urgency-soon" id="metric-vms">0</span>
            </div>
            <div class="h-3 w-px bg-tmc-border"></div>
            <div class="flex items-center gap-2">
                <span class="text-[10px] font-mono text-tmc-muted uppercase">Prophecies</span>
                <span class="text-sm font-mono font-bold text-white" id="metric-prophecies">0</span>
            </div>
            <div class="h-3 w-px bg-tmc-border"></div>
            <div class="flex items-center gap-2">
                <span class="text-[10px] font-mono text-tmc-muted uppercase">Hit Rate</span>
                <span class="text-sm font-mono font-bold text-status-verified" id="metric-hitrate">—</span>
            </div>
            <div class="h-3 w-px bg-tmc-border"></div>
            <div class="flex items-center gap-2">
                <span class="text-[10px] font-mono text-tmc-muted uppercase">Last Tick</span>
                <span class="text-sm font-mono text-tmc-muted" id="metric-lasttick">—</span>
            </div>
        </div>
    </div>

    <!-- ═══════════════ MAIN GRID ═══════════════ -->
    <main class="max-w-[1920px] mx-auto p-6 grid grid-cols-1 lg:grid-cols-3 gap-6" style="height: calc(100vh - 105px);">

        <!-- ── Column 1: AI-Verified Incidents ── -->
        <section class="flex flex-col min-h-0">
            <div class="flex items-center gap-2 mb-3">
                <div class="w-1.5 h-1.5 rounded-full bg-urgency-immediate"></div>
                <h2 class="text-xs font-mono font-semibold uppercase tracking-wider text-tmc-muted">AI-Verified
                    Incidents</h2>
                <span class="text-[10px] font-mono text-tmc-muted ml-auto">Vision Engine</span>
            </div>
            <div class="flex-1 overflow-y-auto terminal-scroll space-y-3" id="incidents-container">
                <div class="text-center text-tmc-muted text-xs font-mono py-12 opacity-60">
                    No active incidents detected
                </div>
            </div>
        </section>

        <!-- ── Column 2: Predictive VMS Copilot ── -->
        <section class="flex flex-col min-h-0">
            <div class="flex items-center gap-2 mb-3">
                <div class="w-1.5 h-1.5 rounded-full bg-urgency-soon"></div>
                <h2 class="text-xs font-mono font-semibold uppercase tracking-wider text-tmc-muted">Predictive VMS
                    Copilot</h2>
                <span class="text-[10px] font-mono text-tmc-muted ml-auto">Physics Engine</span>
            </div>
            <div class="flex-1 overflow-y-auto terminal-scroll space-y-3" id="vms-container">
                <div class="text-center text-tmc-muted text-xs font-mono py-12 opacity-60">
                    No active VMS recommendations
                </div>
            </div>
        </section>

        <!-- ── Column 3: Prophecy Log ── -->
        <section class="flex flex-col min-h-0">
            <div class="flex items-center gap-2 mb-3">
                <div class="w-1.5 h-1.5 rounded-full bg-status-verified"></div>
                <h2 class="text-xs font-mono font-semibold uppercase tracking-wider text-tmc-muted">Camera-to-Camera
                    Prophecy Log</h2>
                <span class="text-[10px] font-mono text-tmc-muted ml-auto">Golden Metric</span>
            </div>
            <div
                class="flex-1 bg-tmc-panel rounded-lg border border-tmc-border panel-glow overflow-hidden flex flex-col">
                <!-- Stats header -->
                <div
                    class="px-4 py-2 border-b border-tmc-border flex items-center gap-4 text-[10px] font-mono text-tmc-muted bg-tmc-bg/50">
                    <span>✅ <span id="log-verified" class="text-status-verified">0</span></span>
                    <span>❌ <span id="log-failed" class="text-status-failed">0</span></span>
                    <span>⏳ <span id="log-pending" class="text-status-pending">0</span></span>
                    <span>🗑 <span id="log-expired" class="text-status-expired">0</span></span>
                </div>
                <!-- Terminal -->
                <div class="flex-1 overflow-y-auto terminal-scroll p-4 font-mono text-xs leading-relaxed relative"
                    id="prophecy-terminal">
                    <div class="text-tmc-muted opacity-60">
                        $ awaiting prophecy data...
                    </div>
                </div>
            </div>
        </section>
    </main>

    <!-- ═══════════════ DATEX II MODAL ═══════════════ -->
    <div id="datex2-modal" class="fixed inset-0 z-[100] hidden">
        <div class="absolute inset-0 bg-black/60 modal-backdrop" id="modal-backdrop"></div>
        <div class="absolute inset-8 bg-tmc-panel border border-tmc-border rounded-lg flex flex-col panel-glow">
            <div class="flex items-center justify-between px-6 py-3 border-b border-tmc-border">
                <div class="flex items-center gap-3">
                    <h3 class="text-sm font-mono font-semibold">DATEX II Export — NTS Compatible</h3>
                    <span
                        class="text-[10px] font-mono text-tmc-muted bg-tmc-accent/30 px-2 py-0.5 rounded">application/xml</span>
                </div>
                <button id="modal-close"
                    class="text-tmc-muted hover:text-white text-lg transition-colors">&times;</button>
            </div>
            <pre class="flex-1 overflow-auto p-6 font-mono text-xs text-green-400/80 leading-relaxed terminal-scroll"
                id="datex2-content">Loading...</pre>
        </div>
    </div>

    <script src="/static/app.js"></script>
</body>

</html>